#!/usr/bin/env bash
set -euo pipefail
if [ "$#" -lt 1 ]; then
  echo "Usage: keepalive -- COMMAND [ARGS...]"
  exit 2
fi
cmd=( "$@" )
echo "Acquiring inhibitor and running: ${cmd[*]}"
exec systemd-inhibit --what=idle:sleep --why="keepalive wrapper" -- "${cmd[@]}"
